const CARDS_NUM   = 5;
const VALID_SUITS = ['diamonds', 'clubs', 'hearts', 'spades'];
const MIN_RANK    = 1;
const MAX_RANK    = 13;

const cardConst = {
  CARDS_NUM,
  MIN_RANK,
  MAX_RANK,
  VALID_SUITS,
};

module.exports = cardConst;
